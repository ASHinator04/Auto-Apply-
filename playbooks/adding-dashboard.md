# Adding Dashboard Work

1. Read root `AGENTS.md`, `apps/AGENTS.md`, and `apps/web/AGENTS.md`.
2. Confirm the dashboard requirement exists in `PRD.md` or a feature spec.
3. Produce a design and implementation plan before adding UI code.
4. Keep domain rules outside the app layer.
5. Add tests and update documentation before committing.
