# contracts/domain AGENTS

## Purpose

`contracts/domain/` will contain shared domain interface definitions.

## Responsibilities

Define durable concepts such as `Job`, `Resume`, `KnowledgeEntry`, `Application`, and `Provider`.

## Constraints

Do not bind domain contracts to UI, storage, automation, or vendor-specific data shapes.
