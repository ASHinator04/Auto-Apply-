# tests/contract AGENTS

## Purpose

`tests/contract/` will contain tests that verify adapters satisfy documented interfaces.

## Responsibilities

Protect provider, persistence, and automation contracts from breaking core workflows.

## Constraints

Avoid live external dependencies unless the tests are explicitly marked and isolated.

