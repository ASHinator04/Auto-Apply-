# packages/persistence AGENTS

## Purpose

`packages/persistence/` will contain storage abstractions and future storage implementations.

## Responsibilities

Own repository interfaces, migrations if needed, local storage behavior, and data access boundaries.

## Constraints

Do not introduce a database or schema before stack selection and storage decisions are documented.
