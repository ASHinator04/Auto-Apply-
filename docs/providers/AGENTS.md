# docs/providers AGENTS

## Purpose

`docs/providers/` contains provider-specific implementation and operations notes.

## Responsibilities

Document provider API behavior, request and response shapes, limitations, and extension guidance.

## Constraints

Do not redefine canonical architecture. Link back to `ARCHITECTURE.md`, `DECISIONS.md`, and phase
reports when they own the broader design.
