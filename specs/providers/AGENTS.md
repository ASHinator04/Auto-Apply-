# specs/providers AGENTS

## Purpose

`specs/providers/` contains provider-specific requirements and verification notes.

## Responsibilities

Keep specs focused on provider boundaries, supported filters, source data, non-goals, and test
expectations.

## Constraints

Specs are not implementation code. Keep them aligned with `specs/search/SPEC.md` and phase reports.
