[project]
name = "job-agent-api"
version = "0.0.0"
description = "FastAPI infrastructure shell for Job Agent."
requires-python = ">=3.13"
dependencies = [
  "fastapi>=0.116.0",
  "pydantic-settings>=2.10.0",
  "python-multipart>=0.0.20",
  "structlog>=25.4.0",
  "uvicorn[standard]>=0.35.0",
]
